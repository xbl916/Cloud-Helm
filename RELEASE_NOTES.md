# Cloud Helm 0.8.1

## 中文操作审计

- H5 和企业微信小程序的审计页面改为显示中文操作、对象类型、执行结果和常见详情，`system` 显示为“系统”。
- 数据库继续保留稳定的英文动作代码，审计 API 同时返回原始字段和中文字段，历史记录无需迁移即可显示中文。
- 镜像、容器 ID、企微 UserId 和外部系统原始错误保留原值，避免技术信息被翻译后失真。

## 按真实订阅模拟告警

- 每条告警规则新增“模拟”按钮，仅全局管理员可用。
- 模拟前检查全局企微通知开关、规则启用状态和规则通知开关，再从规则范围内选择一个匹配节点或容器。
- 接收人使用与真实告警完全相同的账号状态、资源查看权限和告警订阅逻辑；环境/节点订阅覆盖其下资源，项目/容器订阅只覆盖对应容器。
- 模拟消息明确显示规则、模拟资源、操作人与“不代表真实故障”，不会创建真实告警事件、改变连续次数或活动状态。
- 发送结果、模拟资源、接收人数和接收人显示名写入操作审计；同一管理员对同一规则每 30 秒最多发送一次。

## 通知去向

- 真实告警通过现有企业微信自建应用消息发送，目标成员必须处于应用可见范围。
- 全局 `CLOUDHELM_ALERT_NOTIFICATIONS_ENABLED`、告警规则“通知”和成员资源订阅三层都开启才会发送。
- 全部资源账号使用统一订阅；受限账号按环境、节点、Compose 项目或容器订阅，发送前重新校验当前权限并自动去重。

## 升级与发布

- 从 0.8.0 升级无数据库结构变更，不需要修改 `.env`、`agent.env` 或 Compose 参数；只需更新 Server 镜像即可获得本次功能。
- 0.8.0 Agent 与 0.8.1 Server 完全兼容；仍发布同版本 Agent 镜像供版本统一。
- `cloudhelm-0.8.1.tar.gz` 与 `.sha256`：架构无关源码、部署文件、小程序代码及校验文件。
- `ghcr.io/xbl916/cloud-helm-server:0.8.1`：`linux/amd64`、`linux/arm64`。
- `ghcr.io/xbl916/cloud-helm-agent:0.8.1`：`linux/amd64`、`linux/arm64`、`linux/arm/v7`；GPU overlay 仅要求 amd64/arm64。

发布产物不包含 `.env`、企微 Secret、小程序 Secret、数据库、自动备份、Agent 注册令牌或节点身份文件。
