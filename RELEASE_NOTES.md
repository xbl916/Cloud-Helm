# Cloud Helm 0.6.4

## 权限变更安全

- H5 与企业自用小程序在保存权限前先由服务端计算差异，明确显示新增、移除、调整和新授予资源管理权的数量，再进行二次确认。
- 每个成员的权限配置新增单调递增版本号。管理员保存时必须携带读取到的版本；并发编辑发生冲突时返回 `409`，不会静默覆盖另一位管理员的修改。
- 新增有效权限查询，可查看成员在每个节点和容器上的最终查看、日志、运维与管理权限及授权来源。
- 新增资源管理员反查接口，可列出某节点或容器的全局管理员、范围管理员及命中规则。
- 用户列表显示资源管理授权数量；全局管理员可一键撤销某成员的全部资源管理权，同时保留其查看、日志和运维权限。

## 数据库升级保护

- 原有兼容列检查改为 `schema_migrations` 版本化迁移，SQLite 与 PostgreSQL 均会记录已完成版本并支持幂等启动。
- SQLite 在升级前后执行完整性检查；升级前使用 SQLite backup API 在数据库旁创建 `cloudhelm.db.pre-0.6.4.bak` 一致性备份，同一版本不会重复创建。
- 现有用户新增从 `1` 开始的 `access_version`，其他用户、企微身份、授权、节点、容器、历史指标、Agent 凭据和审计数据保持不变。
- 此版本不增加必填环境变量。从 0.6.3 升级只需更新 Server 镜像；Agent 0.6.3 仍兼容，统一版本管理时可以同步升级。

## 页面兼容性

- H5 与小程序均接入权限冲突提示、变更预览和紧急撤销入口。
- 手机端长用户名、资源名、容器名和镜像名允许自动换行，并保留 480px、390px 和 360px 级别的窄屏布局约束。

## 发布产物

- `cloudhelm-0.6.4.tar.gz`：架构无关源码、部署文件与小程序代码。
- `cloudhelm-0.6.4.tar.gz.sha256`：源码包完整性校验。
- `ghcr.io/xbl916/cloud-helm-server:0.6.4`：`linux/amd64`、`linux/arm64`。
- `ghcr.io/xbl916/cloud-helm-agent:0.6.4`：`linux/amd64`、`linux/arm64`、`linux/arm/v7`；GPU overlay 仅要求 amd64/arm64。

发布产物不包含 `.env`、企微 Secret、小程序 Secret、数据库、自动备份、Agent 注册令牌或节点身份文件。
