# Cloud Helm 0.8.0

## 扩展监控告警

- 新增 GPU 使用率、显存使用率、温度以及掉卡/`nvidia-smi` 异常告警。GPU 温度和掉卡默认启用；训练场景容易长期满载，因此使用率和显存规则默认创建但关闭。
- GPU 掉卡使用节点历次正常上报的最大卡数作为持久基线，单卡消失、监控命令异常和已有 GPU 节点误用非 GPU Agent 配置都可识别；全局管理员可在节点 GPU 区域二次确认后重设基线，操作写入审计日志。
- 新增节点网络流量突增告警：动态基线先预热 4 次心跳，当前收发总流量至少 10 MiB/s 才参与判断，默认阈值为高于基线 300%，规则默认关闭。
- 新增容器可写层增长告警。Agent 只在真实 `docker system df` 刷新时按完整采样间隔计算 MiB/s，首次及缓存心跳不产生增长值，默认阈值 1 MiB/s，规则默认关闭。
- 新增容器非零退出码、按逻辑 CPU 数归一化的宿主机 1 分钟负载、Swap 使用率以及宿主机指标连续采集失效告警，默认启用。
- 告警消息增加 `%`、`°C`、`MiB/s` 和 GPU 数量等单位；H5 告警规则列表显示全部新增指标名称。

## 默认值与兼容性

- GPU 温度：85°C、连续 2 次、严重；GPU 掉卡：缺少至少 1 张、连续 2 次、严重。
- 宿主机负载：1 分钟 load average ÷ CPU 核数达到 150%、连续 4 次；Swap：80%、连续 3 次。
- 容器异常退出：状态为 `exited/dead` 且退出码非 0，首次观察即触发。
- 默认规则增加持久播种记录：升级只补充缺少指标的规则，不覆盖已有同指标规则；自本版本起删除规则后不会因 Server 重启被自动恢复。
- 0.8.0 Server 兼容旧 Agent；宿主机归一化负载和容器磁盘增长需要同步升级 0.8.0 Agent，其余新增告警可继续使用既有心跳字段。

## 数据库升级保护

- 新增 `0007_extended_alert_metrics` 幂等迁移，增加 GPU/网络基线和容器磁盘增长速率列，并创建 `alert_rule_seeds` 表；SQLite 与 PostgreSQL 均支持。
- SQLite 迁移前创建 `cloudhelm.db.pre-0.8.0.bak` 一致性备份，既有用户、企微身份、资源权限、告警订阅、节点凭据、指标历史与审计记录保持不变。
- `.env` 和 `agent.env` 均无新增必填变量；Agent 升级不需要重新注册或删除 `deploy/cloudhelm-data`。

## 发布产物

- `main` 的 CI 成功后自动创建缺失的 `v0.8.0` 标签，再触发多架构 Release 流水线；已存在标签不会被覆盖。
- `cloudhelm-0.8.0.tar.gz` 与 `.sha256`：架构无关源码、部署文件、小程序代码及校验文件。
- `ghcr.io/xbl916/cloud-helm-server:0.8.0`：`linux/amd64`、`linux/arm64`。
- `ghcr.io/xbl916/cloud-helm-agent:0.8.0`：`linux/amd64`、`linux/arm64`、`linux/arm/v7`；GPU overlay 仅要求 amd64/arm64。

发布产物不包含 `.env`、企微 Secret、小程序 Secret、数据库、自动备份、Agent 注册令牌或节点身份文件。
